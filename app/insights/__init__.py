"""What each owner likes, learned from what they did -- not from what they said.

`events.record()` keeps every vote (tap, revision, publish). `profile.taste()`
turns the votes into a small, honest summary the agent is shown. `suggest`
uses the summary, the calendar and the unused product photos to propose the
post before the owner has to think of one.
"""
